"""Consumer (Silver/Gold) layers, organized by business subject / data product.

Bronze is source-aligned (see :mod:`analytics_engineer.data_sources`). From Silver
on, the unit of work is a *subject* built by joining sources — e.g. ``frequentation``.
"""
